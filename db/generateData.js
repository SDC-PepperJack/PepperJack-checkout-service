const faker = require('faker');

const generateData() = () => {
  // for loop from 0 to 10 million, but seems unefficient
  // negative while loop, not sure about efficiency but seems unefficient

};

generateData()